// Ternary Function - a shortcut to if{} and else{} statements . helps to assign a variable based on a condition 
//                     condition ? codeIfTrue : codeIfTrue ;


let age=12;
let message = age>=18 ? "You're are adult" : "You're a minor";

console.log(message);